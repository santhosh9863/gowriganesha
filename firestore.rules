rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Tighten security rules before production deployment
    // For development only — allows all read/write access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
