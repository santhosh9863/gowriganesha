rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anonymous authentication required for all read/write operations.
    // Prevents direct Firestore REST API access.
    // Deployed: June 2026
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
